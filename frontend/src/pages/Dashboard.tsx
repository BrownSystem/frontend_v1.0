import { LayoutDashboard } from "../components/layout";

const Dashboard = () => {
  return (
    <div className="w-screen h-screen font-outfit  bg-[var(--brown-ligth-100)]/70">
      <LayoutDashboard />
    </div>
  );
};

export default Dashboard;
